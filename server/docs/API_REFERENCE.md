# API Reference

Moved to [`docs/API.md`](../../docs/API.md) at the repo root — that's now
the single canonical API reference. This file previously duplicated it with
response-shape examples that had drifted from what the code actually
returns; keeping one copy in sync is the point.
